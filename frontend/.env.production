# 配送小智 · 前端生产环境变量
#
# ⚠️ VITE_ 前缀的变量会暴露到浏览器 bundle 里。
#    这里只允许保存"公开的后端 URL"，禁止保存任何：
#    - Coze / 豆包 / DeepSeek API Key
#    - JWT Secret / 数据库密码
#    - GitHub Token / 业务数据
#
# 这些密钥都应放在 Render / Coze 控制台，仅在 backend/src/config.js 里通过 process.env 读取。
#
# VITE_API_BASE_URL 由 GitHub Actions 在 build 时通过 Secret 注入（见 .github/workflows/deploy-frontend.yml）。
# 这里只写默认值占位，CI 会覆盖。
VITE_APP_TITLE=九小智 · AI 配送运营智能决策系统
VITE_API_BASE_URL=https://peisong-backend.onrender.com
VITE_USE_MOCK=false
